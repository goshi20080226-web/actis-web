ACTIS CSS rebuild

index.css was rebuilt as a coherent visual layer while retaining the existing stylesheet
before the ACTIS VISUAL SYSTEM REBUILD section.

The rebuild restores:
- dark ACTIS sidebar / white content shell
- restrained cards and controls
- consistent typography and spacing
- responsive layout
- passenger timetable styling without horizontal scrolling
- dataset selector styling

No JSX/JS files were intentionally changed by this CSS-only rebuild.
